<Window x:Class="HitePhoto.PrintStation.UI.GetFromProductionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Get from Production" Width="520" Height="520"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource WindowBg}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock x:Name="OrderHeader" FontSize="16" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimary}"/>
            <TextBlock x:Name="OrderDetail" FontSize="12"
                       Foreground="{DynamicResource TextMuted}" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Folders section -->
        <StackPanel Grid.Row="1" Margin="0,0,0,4">
            <TextBlock Text="Folders to download:" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimary}"/>
            <StackPanel x:Name="FolderPanel" Margin="8,4,0,0"/>
            <TextBlock x:Name="FolderStatus" FontSize="11"
                       Foreground="{DynamicResource TextMuted}" Margin="8,2,0,0"/>
        </StackPanel>

        <!-- Item checkboxes grouped by size -->
        <Border Grid.Row="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                CornerRadius="2" Margin="0,4,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ItemPanel" Margin="8,4"/>
            </ScrollViewer>
        </Border>

        <!-- Create order toggle -->
        <CheckBox x:Name="CreateOrderCheck" Grid.Row="3" Content="Create receive order (-R#)"
                  IsChecked="True" Margin="0,8,0,0" FontSize="12"
                  Foreground="{DynamicResource TextPrimary}"/>

        <!-- Comment -->
        <Grid Grid.Row="4" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Message:" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
            <TextBox x:Name="CommentBox" Grid.Column="1" Margin="4,0,0,0"
                     FontSize="12" VerticalAlignment="Center"/>
        </Grid>

        <!-- Action buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock x:Name="SummaryText" VerticalAlignment="Center" Margin="0,0,12,0"
                       FontSize="11" Foreground="{DynamicResource TextMuted}"/>
            <Button x:Name="GetBtn" Content="Get" Width="100" Height="32"
                    Click="Get_Click" IsEnabled="False" FontWeight="SemiBold"/>
            <Button Content="Cancel" Width="80" Height="32" Margin="8,0,0,0"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
